<?php
$tags = get_tags();
if ($tags) {
            foreach ($tags as $tag) {
                    echo '<p>Tag : <a href="'" . get_tag_link ($tag->term_id ) . '" tittle= "' . sprintf ( _( "View
            }
}
?>

<?php
$tags = wp_get_post_tags($post -> ID);
if ($tags) {
            foreach($tags as $tag) {
                        echo '<p>' . $title . <a href="' . get_term_link ($tag, 'post_tag' ) . '" title="'  . sprin
            }
}
?>
